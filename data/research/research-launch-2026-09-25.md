# Autonomous research launch marker

Temporary PR event used to start the keyless public-web research pipeline.

The workflow checks out `dev`, performs discovery, verification, normalization and replanning, and writes research state only to `dev`.

Parser fix: provider redirect extraction v1.2; semantic replanning enabled.
