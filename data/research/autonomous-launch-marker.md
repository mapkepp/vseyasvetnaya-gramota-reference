# Autonomous research launch

This marker intentionally triggers the production scheduler's `push` event. The scheduler checks out `dev` and writes research state only to `dev`.

Backend: multi-provider keyless public-web worker.
